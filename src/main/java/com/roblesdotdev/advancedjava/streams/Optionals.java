package com.roblesdotdev.advancedjava.streams;

// Exist or not

// Optional<T> wraps an object of type T or no object.
// Safe alternative ot null
// Only safe if use right

// Use alternative value

// optionalString.orElse("")
// optionalString.orElseGet(() -> System.getProperty("user.dir"))
// optionalString.orElseThrow(IllegalStateException::new)

// Only when present

// optionalString.ifPresent(results::add)
// values.map(results::add)

import java.util.Optional;

public class Optionals {
    public static void main(String[] args) {


    }
}
